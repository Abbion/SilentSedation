version: '3'
services:
    app:
      build:
        context: .
        dockerfile: ./docker/vue/Dockerfile
      ports:
        - 8080:80
      
    nginx:
      image: nginx:alpine
      volumes:
        - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
      ports:
        - 80:80
      depends_on:
        - app